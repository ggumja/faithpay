# SoulPay 작업 진행 및 핸드오버 기록 (2026-09-10)

## 📌 현재 브랜치 및 배포 상태
- **현재 작업 브랜치**: `dev` (최신 커밋: `1ca5975`, 작업 트리 완전 Clean)
- **상용 브랜치 (`main`)**: `dev` 머지 및 푸시 완료 (머지 커밋: `a56a621`)
- **배포 URL**:
  - Dev/Staging: https://dev.soulpay.kr/system/admin/ledger
  - 로컬 테스트: http://localhost:5173/system/admin/ledger
- **백엔드 Supabase Edge Function**: `make-server-d0d82cc7` 최신 배포 완료

---

## 🛠️ 오늘 완료된 작업 내역

### 1. 거래원장 오늘 결제분 누락 원인 규명 및 복구
- **현상**: `donations` 테이블에는 오늘 결제 2건(각원사 1,000원 2건)이 있었으나, 거래원장(`/system/admin/ledger`)에는 결제분이 표출되지 않음.
- **원인**: 나노페이 일반 인증결제(Cert) 결제 완료 콜백(`handleCertCallback`) 및 `updateDonation` 처리 시 원장 기록 로직(`recordDonationToLedger`)이 누락되어 있었음.
- **해결 조치**:
  - `database.tsx`의 `getAdminSettlementLedger`에 **자동 정합성 동기화(Auto-Reconcile)** 로직 구축 (누락된 결제건 자동 감지 및 실시간 원장 기입).
  - `updateDonation` 및 `handleCertCallback`에서 결제 완료 시 `recordDonationToLedger` 자동 호출 연동.
  - 날짜 필터 조회 시 당일 종료시각(`23:59:59.999Z`) 보정으로 '오늘' 필터 조회 시 결제분 누락 방지.

### 2. 통합 결제·거래 원장 (Transaction Ledger) 전면 재구성
- **개편 배경**: 기존 화면은 계약수수료(3%), PG원가(1.5%), 대리점/영업자 수수료 등 정산 배분 위주의 화면이었음. 사용자의 "수수료가 아닌 결제 거래 디테일 정보가 담긴 말그대로 거래원장" 요청 반영.
- **피드백 반영**: **기도제목 컬럼은 완전히 제외**하고 결제 거래 핵심 정보에 집중.
- **재구성된 주요 요소**:
  - **상단 KPI 실시간 요약 카드**: 오늘 결제액/건수, 이번달 누적액/건수, 취소·환불 누적액/건수, 정기결제 비중(%).
  - **다차원 필터 & 검색**: 날짜 퀵 필터(오늘/7일/이번달/전체/직접지정), 결제상태 필터, 결제유형 필터, 가맹단체 드롭다운, 실시간 통합 검색, **CSV 내보내기(다운로드)**.
  - **거래 테이블 컬럼**:
    - 거래일시 (KST)
    - 상태 배지 (🟢 결제완료 / 🔴 결제취소 / ⚠️ 승인실패 / 🟡 결제대기)
    - 가맹단체 (단체명 + 슬러그 + 종교/단체 구분 뱃지: 불교·사찰, 개신교·교회 등)
    - 봉헌 / 공덕 항목명
    - 거래금액 (통화 포맷, 취소 시 취소선 및 환불액 표기)
    - 결제유형 (단건결제, 정기결제, 키오스크)
    - 결제수단 / PG (신용카드 등 + 나노페이/토스페이먼츠 뱃지)
    - 기부자 / 신도 (성명 + 연락처 + 세례명/법명)
    - 식별 / 승인번호 (거래번호 Donation ID, PG TID, 승인번호 Approve No)
    - [상세] 버튼 ➔ **거래 상세 전문 모달** (결제 승인 식별번호 복사, 가맹점/기부자 상세, 취소사유, PG 매출전표 바로가기 링크).
  - **백엔드 매핑**: `database.tsx`의 `getAllDonations`에서 `tenants` 테이블(`name`, `slug`, `religion_type`)을 실측 조인/매핑하도록 개선.

### 3. 신규 가맹단체 등록 폼 개선
- `SystemAdminTenantCreate.tsx`: 신규 단체 개설 시 슬러그 및 단체 소개글 기본값 제거 ➔ **완전 빈칸(Clean State)**으로 표출.

### 4. Cloudflare Pages 빌드 안정화
- `scripts/postbuild.mjs`: Cloudflare Pages 타깃별 빌드(`dist`, `dist/dev`, `dist/ops` 등) 시 `404.html` 및 `.nojekyll`을 자동으로 감지하여 생성하도록 구축하여 배포 에러 원천 해결.

---

## 📂 주요 변경 파일 목록
1. `SoulPay SaaS Platform/src/app/pages/admin/components/MultiPartySettlementLedger.tsx`: 통합 결제·거래 원장 컴포넌트 전면 재작성
2. `SoulPay SaaS Platform/supabase/functions/make-server-d0d82cc7/database.tsx`:
   - `recordDonationToLedger` 멱등성 및 일시/PG사 매핑 보완
   - `updateDonation` 결제완료 시 자동 원장 기입
   - `getAdminSettlementLedger` 자동 정합성 동기화(Auto-Reconcile) 및 날짜 범위 보정
   - `getAllDonations` 테넌트 정보(`name`, `slug`, `religion_type`) 실측 매핑
3. `SoulPay SaaS Platform/supabase/functions/make-server-d0d82cc7/index.tsx`: `handleCertCallback` 결제 완료 시 원장 기입 보강
4. `SoulPay SaaS Platform/src/app/pages/admin/SystemAdminTenantCreate.tsx`: 신규 등록 폼 초기값 빈칸화
5. `SoulPay SaaS Platform/package.json` & `scripts/postbuild.mjs`: Cloudflare Pages 빌드 SPA 404 폴백 스크립트

---

## 💡 내일 이어서 진행할 수 있는 작업 후보
1. **정산 센터 (`/system/admin/settlements`) 내 4자간 수수료 분구 탭 정리**:
   - 수수료 배분 내역이 필요할 때 정산 센터 메뉴 전용 탭에서 확인할 수 있도록 네비게이션 정리.
2. **거래원장 상세 모달 내 결제 취소(환불) 실행 버튼 연동**:
   - 관리자가 거래원장 모달에서 직접 즉시 승인취소(환불) API를 호출할 수 있는 기능 연동.
3. **가맹점별(Tenant Admin) 거래원장 화면 동기화**:
   - 개별 가맹단체 관리자 페이지(`/:tenant/admin/history`)에도 개선된 거래 디테일 레이아웃 적용 검토.
