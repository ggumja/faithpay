{
  "name": "soulpay-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm --prefix \"FaithPay SaaS Platform\" run build",
    "dev": "pnpm --prefix \"FaithPay SaaS Platform\" run dev"
  }
}
